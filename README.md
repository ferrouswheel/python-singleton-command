python-singleton-command
========================

A decorator for ensuring only one instance of a command can run on a machine.

Useful for ensuring non-overlapping cron jobs that ocassionally run longer than expected.
